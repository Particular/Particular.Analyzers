assembly-versioning-scheme: MajorMinorPatch
branches:
  master:
    mode: ContinuousDeployment
    tag: alpha
    increment: Minor
    tracks-release-branches: true
  release:
    increment: Patch
    tag: ''
